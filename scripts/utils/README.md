# scripts/utils

This directory contains utility functions shared by multiple scripts.

Those helpers mostly abstract common Node.js operations needed by build and test
scripts, such as process execution, filesystem access and manifest parsing.

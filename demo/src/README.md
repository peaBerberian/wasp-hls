# demo source code

This directory contains the source code of the demo application.

Its `index.tsx` file is the application's entry point. It initializes React,
exposes some debugging helpers on `window` and renders the player UI.

The code is split into the following directories:

- `components`: React components making up the demo's user interface.

- `utils`: small helpers only used by the demo.
